---
/* ============================================================
   PAGE HEADER — encabezado de páginas internas.
   Fondo oscuro industrial + breadcrumb + título. Consistente
   con la identidad del Home.
   ============================================================ */
interface Crumb { label: string; href?: string; }
interface Props {
  eyebrow?: string;
  title: string;
  subtitle?: string;
  breadcrumbs?: Crumb[];
  /** 'light' (por defecto, fondo claro) o 'dark' */
  tone?: 'dark' | 'light';
}
const { eyebrow, title, subtitle, breadcrumbs = [], tone = 'light' } = Astro.props;
---
<header class:list={['page-header', `page-header--${tone}`]}>
  <div class="page-header__bg" aria-hidden="true">
    <div class="page-header__glow"></div>
  </div>
  <div class="container page-header__inner">
    {breadcrumbs.length > 0 && (
      <nav class="breadcrumb" aria-label="Ruta de navegación">
        <ol>
          <li><a href="/">Inicio</a></li>
          {breadcrumbs.map((c) => (
            <li>
              {c.href ? <a href={c.href}>{c.label}</a> : <span aria-current="page">{c.label}</span>}
            </li>
          ))}
        </ol>
      </nav>
    )}
    {eyebrow && <p class="eyebrow" data-reveal>{eyebrow}</p>}
    <h1 class="page-header__title h-xl" data-reveal>{title}</h1>
    {subtitle && <p class="page-header__sub text-l" data-reveal>{subtitle}</p>}
  </div>
</header>

<style>
  .page-header {
    position: relative;
    background: var(--gradient-hero);
    color: var(--text-on-dark);
    padding-top: calc(var(--header-h) + var(--space-10));
    padding-bottom: var(--space-14);
    overflow: hidden;
    isolation: isolate;
  }
  .page-header__bg { position: absolute; inset: 0; z-index: -1; }
  .page-header__glow {
    position: absolute;
    top: -30%;
    right: -10%;
    width: 50%;
    height: 140%;
    background: var(--gradient-glow);
  }
  .page-header__inner { display: flex; flex-direction: column; gap: var(--space-5); max-width: 820px; }
  .page-header .eyebrow { color: var(--celeste-300); }
  .page-header__title { color: #fff; }
  .page-header__sub { color: var(--azul-100); max-width: 62ch; }

  .breadcrumb ol { display: flex; flex-wrap: wrap; gap: var(--space-3); align-items: center; }
  .breadcrumb li { display: flex; align-items: center; gap: var(--space-3); font-size: var(--fs-text-s); color: var(--azul-200); }
  .breadcrumb li + li::before { content: '/'; color: var(--azul-300); }
  .breadcrumb a { color: var(--azul-100); transition: color var(--dur-fast) var(--ease-out); }
  .breadcrumb a:hover { color: var(--celeste-300); }
  .breadcrumb [aria-current] { color: #fff; }

  /* ---- Variante clara (fondo blanco) ---- */
  .page-header--light {
    background: var(--bg-base);
    color: var(--text-body);
    border-bottom: 1px solid var(--border);
  }
  .page-header--light .page-header__glow {
    background: radial-gradient(60% 60% at 80% 20%, rgba(0, 172, 229, 0.10) 0%, transparent 70%);
  }
  .page-header--light .eyebrow { color: var(--accent); }
  .page-header--light .page-header__title { color: var(--text-strong); }
  .page-header--light .page-header__sub { color: var(--text-body); }
  .page-header--light .breadcrumb li { color: var(--text-muted); }
  .page-header--light .breadcrumb li + li::before { color: var(--neutro-250); }
  .page-header--light .breadcrumb a { color: var(--text-body); }
  .page-header--light .breadcrumb a:hover { color: var(--accent); }
  .page-header--light .breadcrumb [aria-current] { color: var(--text-strong); }
</style>
