---
/* Iconos por servicio (line-icons, stroke = currentColor). */
interface Props { slug: string; size?: number; }
const { slug, size = 28 } = Astro.props;
const common = { width: size, height: size, viewBox: '0 0 24 24', fill: 'none', 'aria-hidden': 'true' as const };
---
{slug === 'integridad-estructural' && (
  <svg {...common}><path d="M3 21h18M5 21V8l7-5 7 5v13M9 21v-6h6v6" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>
)}
{slug === 'monitoreo-mantenimiento-predictivo' && (
  <svg {...common}><path d="M3 12h3l2-7 4 14 2-7h7" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>
)}
{slug === 'ingenieria-de-confiabilidad' && (
  <svg {...common}><path d="M12 3v3m0 12v3m9-9h-3M6 12H3m13.5-6.5l-2 2m-5 5l-2 2m9 0l-2-2m-5-5l-2-2" stroke="currentColor" stroke-width="1.6" stroke-linecap="round"/><circle cx="12" cy="12" r="3.2" stroke="currentColor" stroke-width="1.6"/></svg>
)}
{slug === 'metalurgia-inteligente' && (
  <svg {...common}><path d="M9 3h6M10 3v5l-5 9a2 2 0 002 3h10a2 2 0 002-3l-5-9V3M7.5 14h9" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>
)}
{slug === 'confiabilidad-inteligente' && (
  <svg {...common}><rect x="4" y="4" width="16" height="16" rx="2.5" stroke="currentColor" stroke-width="1.6"/><path d="M9 2v2m6-2v2M9 20v2m6-2v2M2 9h2m-2 6h2m16-6h2m-2 6h2M9.5 9.5h5v5h-5z" stroke="currentColor" stroke-width="1.6" stroke-linecap="round"/></svg>
)}
{slug === 'ingenieria-digital' && (
  <svg {...common}><rect x="3" y="4" width="18" height="13" rx="2" stroke="currentColor" stroke-width="1.6"/><path d="M8 21h8m-4-4v4M8.5 9l-2 2.5 2 2.5m7-5l2 2.5-2 2.5" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>
)}
{slug === 'gestion-de-reparables-criticos' && (
  <svg {...common}><path d="M21 16V8a2 2 0 00-1-1.73l-7-4a2 2 0 00-2 0l-7 4A2 2 0 003 8v8a2 2 0 001 1.73l7 4a2 2 0 002 0l7-4A2 2 0 0021 16z" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round"/><path d="M3.3 7L12 12l8.7-5M12 22V12" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round"/></svg>
)}
